# render.yaml - Render deployment configuration
services:
  - type: web
    name: watchtogether
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true